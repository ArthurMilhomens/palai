$kind: http-request
name: Pal Drops
method: GET
url: '{{baseUrl}}/v1/pals/{{palIdOrSlug}}/drops'
description: Lista os itens que um Pal pode dropar ao ser derrotado.
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
