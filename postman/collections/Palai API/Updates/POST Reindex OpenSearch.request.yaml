$kind: http-request
name: Reindex OpenSearch
method: POST
url: '{{baseUrl}}/v1/updates/index'
description: '[Admin] Reindexa a versão ativa do jogo no OpenSearch. Requer role Admin.'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
