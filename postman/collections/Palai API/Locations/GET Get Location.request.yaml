$kind: http-request
name: Get Location
method: GET
url: '{{baseUrl}}/v1/locations/{{locationIdOrSlug}}'
description: Retorna detalhes de um local específico.
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
