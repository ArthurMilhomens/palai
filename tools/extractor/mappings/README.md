# Mappings (.usmap)

Coloque aqui o arquivo `Mappings.usmap` do Palworld (UE5.1), ou rode:

```bash
npm run extract -- --download-mappings
```

Os `.usmap` não são commitados (arquivo grande / muda por patch).
